{
  "name": "frontend-rdj",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm install --no-audit --no-fund --include=optional && npm run build && cd ../server && npm install --no-audit --no-fund --include=optional",
    "build:client": "cd client && npm install --no-audit --no-fund --include=optional && npm run build",
    "install:server": "cd server && npm install --no-audit --no-fund --include=optional",
    "install:client": "cd client && npm install --no-audit --no-fund --include=optional",
    "start": "node server/server.js",
    "dev:api": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "dev": "npm run dev:api & npm run dev:client"
  },
  "engines": {
    "node": "20.x"
  }
}

{
  "name": "frontend-rdj",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm install --no-audit --no-fund && npm run build && cd ../server && npm install --no-audit --no-fund",
    "build:client": "cd client && npm install --no-audit --no-fund && npm run build",
    "install:server": "cd server && npm install --no-audit --no-fund",
    "install:client": "cd client && npm install --no-audit --no-fund",
    "start": "node server/server.js",
    "dev:api": "npm run dev -w server",
    "dev:client": "npm run dev -w client",
    "dev": "npm run dev:api & npm run dev:client"
  },
  "engines": {
    "node": "20.x"
  }
}
